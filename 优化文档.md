## 一、精简 `bg-remover` 页面冗余提示

**问题**：页面顶部存在两条“使用建议”提示：

- 一条是纯文本（位于标题下方）
- 另一条是带底色、图标的醒目提示（位于操作区上方）

**影响**：信息重复，视觉冗余，降低页面整洁度。

**操作**：

- 删除纯文本版本，只保留醒目的带底色提示条。

- 保留的提示条文案建议改为：

  > 💡 **使用建议**：智能抠图依赖 WebGPU 加速，电脑端 Chrome/Edge 浏览器可获得最快处理速度。手机端同样可用，但首次加载模型可能稍慢。

- 样式保持浅灰色圆角背景，无需修改。

------

## 二、提升移动端体验

**问题**：部分交互元素在手机端可能点击区域过小或布局偏移（特别是 `bg-remover` 的下载按钮、滑块等）。

**操作**：

- 在 iPhone 或 Android 真机上测试以下核心工具：
  - 图片压缩：上传按钮、质量滑块、下载按钮。
  - 智能抠图：上传区域、涂抹画笔、下载按钮。
  - PDF 合并：文件上传区域、合并按钮。
- 确保所有可点击元素的最小触控区域为 `44×44px`（已满足则无需调整）。
- 检查 `bg-remover` 页面的提示条在手机上的折行效果，确保不遮挡重要操作。

------

## 三、修复 `about.html` 的文本溢出问题

**问题**：在某些桌面浏览器宽度下（例如 1024px 或更窄），`about.html` 中的长段落可能超出容器边界，出现横向滚动条或文字被截断。

**操作**：

- 为 `about.html` 的内容容器添加 `word-wrap: break-word` 或 `overflow-wrap: break-word` 样式。
- 确保最大宽度容器（`.container`）的 `max-width` 和 `padding` 设置正确（当前为 `1000px` 和 `48px 24px`）。
- 测试调整浏览器窗口宽度，确认文本始终在容器内自动换行。

|      |      |      |      |
| :--- | :--- | :--- | :--- |
|      |      |      |      |
|      |      |      |      |
|      |      |      |      |
|      |      |      |      |
|      |      |      |      |
|      |      |      |      |
|      |      |      |      |
|      |      |      |      |
|      |      |      |      |
|      |      |      |      |