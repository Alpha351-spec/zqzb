# 杂七杂八工具箱 – 开发文档（V4.2）

## 一、项目定位

- **名称**：杂七杂八工具箱
- **核心价值**：免费、纯本地处理、不传服务器、隐私安全
- **目标用户**：需要快速处理图片、文字、数据的普通网民/打工人/学生

---

## 二、功能列表（V4.2）

### 图片处理类（6个工具）

| 工具         | 功能描述                                       | 处理方式                                |
| ------------ | ---------------------------------------------- | --------------------------------------- |
| 图片压缩     | 上传JPG/PNG/WebP，滑动调节压缩质量，下载       | 浏览器本地（browser-image-compression） |
| 图片裁剪     | 支持按比例或自由裁剪，四角拖拽手柄             | 原生Canvas + JS                         |
| 图片格式转换 | PNG/JPG/WebP互转                               | 原生Canvas + JS                         |
| 图片水印添加 | 文字水印+图片水印，支持平铺、透明度调节、自定义位置/颜色/大小 | 原生Canvas + JS                         |
| 图片尺寸调整 | 按比例或指定宽高缩放图片                       | 原生Canvas + JS                         |
| **智能抠图** | **AI自动移除背景，透明背景输出，全程本地处理** | **`<background-remover>` 网页组件**     |

### 文本处理类（6个工具）

| 工具           | 功能描述                           | 处理方式 |
| -------------- | ---------------------------------- | -------- |
| 文字去空格     | 去除首尾空格、多余空行、全角转半角 | 纯前端JS |
| 字数统计       | 中文/英文/数字/标点统计            | 纯前端JS |
| 大小写转换     | 全大写、全小写、首字母大写、驼峰   | 纯前端JS |
| JSON格式化     | 一键格式化/压缩/校验               | 纯前端JS |
| 文字对比       | 比较两段文本差异，高亮显示         | 纯前端JS |
| 正则表达式测试 | 输入正则和测试文本，实时匹配高亮   | 纯前端JS |

### 编码转换类（4个工具）

| 工具         | 功能描述                | 处理方式             |
| ------------ | ----------------------- | -------------------- |
| Base64编解码 | 文本转Base64或反过来    | 纯前端JS             |
| URL编解码    | 中文和特殊字符编码      | 纯前端JS             |
| Unicode转换  | 中文转\u格式或反过来    | 纯前端JS             |
| 颜色选择器   | 拾色器、RGB/HEX/HSL转换 | 原生input color + JS |

### 生成工具类（5个工具）

| 工具         | 功能描述                            | 处理方式  |
| ------------ | ----------------------------------- | --------- |
| 二维码生成   | 输入文本/链接，生成二维码，可加Logo | qrcode.js |
| 随机密码生成 | 自定义长度和字符类型                | 纯前端JS  |
| 时间戳转换   | Unix时间戳与日期时间互转            | 纯前端JS  |
| 随机数生成器 | 生成指定范围随机整数/小数           | 纯前端JS  |
| UUID生成器   | 生成UUID v4，支持批量生成           | 纯前端JS  |

### 文档处理类（12个工具）

| 工具         | 功能描述                           | 处理方式             |
| ------------ | ---------------------------------- | -------------------- |
| PDF合并      | 多个PDF合并为一个文件              | pdf-lib              |
| PDF拆分      | 按页码范围拆分PDF                  | pdf-lib              |
| PDF提取页面  | 提取指定页面为新PDF                | pdf-lib              |
| PDF删除页面  | 移除不需要的页面                   | pdf-lib              |
| PDF旋转      | 旋转全部或指定页面                 | pdf-lib              |
| PDF转图片    | 每页转为PNG/JPG图片                | pdf.js + Canvas      |
| 图片转PDF    | 多张图片合并为PDF                  | pdf-lib              |
| PDF压缩      | 减小PDF文件体积                    | pdf-lib              |
| PDF添加密码  | 为PDF设置密码保护                  | pdf-lib              |
| PDF移除密码  | 输入密码解密PDF                    | pdf-lib              |
| PDF水印      | 添加文字水印保护版权               | pdf-lib              |
| PDF预览      | 在线查看PDF内容                    | pdf.js               |

> **工具总数**：33个（图片处理6 + 文本处理6 + 编码转换4 + 生成工具5 + 文档处理12）

---

## 三、技术架构

- **前端**：HTML5 + CSS3 + 原生JavaScript（无框架）
- **依赖库**：
  - 通用：无框架依赖
  - 图片压缩：`browser-image-compression`
  - 二维码：`qrcode.js`
  - **智能抠图**：`@ligrila/background-remover`（通过CDN引入的Web组件）
  - **PDF处理**：`pdf-lib`（PDF操作）、`pdf.js`（PDF预览和转图片）
- **后端**：无（完全静态）
- **托管**：GitHub Pages（免费静态托管）
- **域名**：初期使用平台免费子域名，后期可绑定廉价域名
- **广告**：Google AdSense（已集成）

---

## 四、文件结构

```
杂七杂八工具箱/
├── index.html                    # 首页（分类导航+工具列表）
├── css/
│   └── style.css                 # 全局样式
├── js/
│   └── common.js                 # 公共JS（页脚年份、今日人数等）
├── image-compress/               # 图片压缩
├── image-crop/                   # 图片裁剪
├── image-format/                 # 图片格式转换
├── image-watermark/              # 图片水印添加
├── image-resize/                 # 图片尺寸调整
├── bg-remover/                   # 智能抠图
│   └── index.html
├── text-trim/                    # 文字去空格
├── word-count/                   # 字数统计
├── case-convert/                 # 大小写转换
├── json-formatter/               # JSON格式化
├── text-diff/                    # 文字对比
├── regex-test/                   # 正则表达式测试
├── base64-tool/                  # Base64编解码
├── url-encode/                   # URL编解码
├── unicode-convert/              # Unicode转换
├── color-picker/                 # 颜色选择器
├── qr-generator/                 # 二维码生成
├── random-password/              # 随机密码生成
├── timestamp/                    # 时间戳转换
├── random-number/                # 随机数生成器
├── uuid-generator/               # UUID生成器
├── pdf/                          # 【新增】PDF处理工具
│   ├── merge/                    # PDF合并
│   ├── split/                    # PDF拆分
│   ├── extract/                  # PDF提取页面
│   ├── delete/                   # PDF删除页面
│   ├── rotate/                   # PDF旋转
│   ├── pdf-to-image/             # PDF转图片
│   ├── image-to-pdf/             # 图片转PDF
│   ├── compress/                 # PDF压缩
│   ├── encrypt/                  # PDF添加密码
│   ├── decrypt/                  # PDF移除密码
│   ├── watermark/                # PDF水印
│   └── preview/                  # PDF预览
├── about.html                    # 关于页面
├── privacy.html                  # 隐私政策页面
├── guide.html                    # 使用指南与常见问题
├── sitemap.xml                   # 站点地图
├── CHANGELOG.md                  # 文档更新记录
└── 开发文档.md                   # 本文件
```

---

## 五、智能抠图功能（方案一）实现说明

### 5.1 技术选型
采用 `@ligrila/background-remover` Web组件，基于 `@imgly/background-removal` 核心库封装，提供开箱即用的UI和逻辑。

### 5.2 集成方式（在 `bg-remover/index.html` 中）

1. **HTML结构**（极简风格，与工具箱统一）：
   - 包含“← 返回工具箱”链接
   - 工具标题和描述
   - `<background-remover>` 标签
   - 隐私说明文字

2. **引入组件**：
   ```html
   <script type="module" src="https://cdn.jsdelivr.net/npm/@ligrila/background-remover@0.3.8/dist/background-remover.es.js"></script>
   ```

3. **自定义属性**（可选）：
   - `data-device="webgpu"` 优先使用WebGPU加速
   - `data-theme="light"` 亮色主题
   - `data-label="点击或拖拽图片"` 自定义提示文字

### 5.3 样式适配
- 组件内部样式已适配，外部只需保证容器宽度（如 `.container`）和留白。
- 可添加极简CSS确保组件与工具箱风格一致（如边框、圆角继承）。

### 5.4 注意事项
- 模型文件（~30MB）在首次使用时下载并缓存在IndexedDB中，后续秒开。
- 所有计算在用户浏览器本地完成，无服务器开销。
- 组件支持WebGPU/WASM自动降级。

---

## 5B、图片水印工具功能说明

### 5B.1 功能概述
图片水印工具支持两种水印类型：文字水印和图片水印，通过Tab标签切换。

### 5B.2 文字水印
- 自定义水印文字内容
- 可调字体大小（12-200px）和颜色
- 支持6种位置：左上、右上、左下、右下、居中、平铺
- 平铺模式：斜45°重复排列，适合版权保护

### 5B.3 图片水印
- 上传PNG/Logo等图片作为水印
- 按底图宽度比例调节水印大小（5%-80%）
- 选择水印图片后即时显示预览缩略图
- 支持同样的6种位置模式

### 5B.4 通用功能
- **透明度控制**：滑动条调节（10%-100%），默认70%
- **位置选择**：左上、右上、左下、右下、居中、平铺
- **平铺实现**：使用Canvas的`save/translate/rotate/restore`实现斜45°重复排列
- **输出格式**：PNG（支持透明通道）

---

## 六、页面设计要求

### 6.1 全局风格

- **留白**：页面内容区上下左右内边距充足（`padding: 48px 24px`），区块之间间距大
- **无阴影**：任何元素不使用`box-shadow`，只允许使用`border`或`border-bottom`
- **圆角**：输入框、按钮、结果区域使用圆角（`border-radius: 12px ~ 24px`）
- **配色**：背景纯白`#ffffff`，文字深灰`#111111`/`#555555`，边框浅灰`#eaeaea`
- **主色调**：按钮颜色`#009DFF`，悬停状态`#0086e0`

### 6.2 首页设计（分类导航模式）

首页采用分类标签+工具列表模式：

1. **顶部区域**：网站标题和副标题
2. **分类导航栏**：横向排列5个分类标签
   - 图片处理（6个工具）
   - 文本处理（6个工具）
   - 编码转换（4个工具）
   - 生成工具（5个工具）
   - 文档处理（12个工具）
3. **工具列表区域**：显示当前分类下的工具卡片
   - 每个卡片包含：工具名称、一句话介绍
   - 点击卡片跳转到对应工具页
4. **其他模块**：
   - 今日使用统计（localStorage持久化，递增显示）
   - 用户评价（气泡动画，自动轮播）
   - 小贴士（横向布局，图标+文本）

### 6.3 工具页面布局

从上到下依次为：

1. **横向导航栏**：显示"← 返回工具箱"和**同分类所有工具链接**（当前页面对应链接添加`active`类）
2. **工具标题**：字号2rem，字重500，底部间距16px
3. **工具简要说明**：字号1rem，颜色灰色，底部间距48px
4. **工具操作区**：包含上传/输入控件、滑块、按钮等（抠图工具直接嵌入组件）
5. **结果展示区**：浅色边框包裹，圆角24px，内边距24px
6. **底部推荐模块（可选）**：其他工具的文本链接

### 6.4 移动端适配

- 导航栏换行，间距缩小为20px
- 容器内边距缩减为`32px 20px`
- 标题字号适当缩小为1.75rem
- 所有输入框宽度100%
- 分类标签换行显示

---

## 七、样式规范速查

| 元素         | 样式要求                         |
| ------------ | -------------------------------- |
| 全局背景     | `#ffffff`                        |
| 正文颜色     | `#111111`                        |
| 辅助文字     | `#555555`                        |
| 边框颜色     | `#eaeaea`                        |
| 主按钮颜色   | `#009DFF`                        |
| 按钮悬停颜色 | `#0086e0`                        |
| 容器最大宽度 | `1000px`，居中                   |
| 容器内边距   | `48px 24px`（移动端`32px 20px`） |
| 工具标题字号 | `2rem`（移动端`1.75rem`）        |
| 工具说明字号 | `1rem`                           |
| 输入框圆角   | `12px`                           |
| 按钮圆角     | `30px`                           |
| 结果区域圆角 | `24px`                           |
| 分类标签圆角 | `20px`                           |
| 工具卡片圆角 | `12px`                           |

---

## 八、关键注意事项

1. **所有工具必须在浏览器本地完成处理**，不得上传服务器（智能抠图组件同样遵守）。
2. **不要使用需要API Key的外部服务**，防止过期失效。
3. **每个页面必须设置独立的`<title>`和`<meta description>`**，包含核心关键词。
4. **隐私政策页面必须写明**："本工具所有处理均在本地进行，不会上传任何文件"。
5. **智能抠图组件**需在页面显式说明：首次使用需下载AI模型（约30MB），后续使用无需再下载。
6. **进度条样式**：使用渐变背景色动态显示当前值，滑块使用白色圆点+蓝色边框。
7. **使用统计**：使用localStorage持久化，每次访问递增1-3人。
8. **导航栏完整性**：每个工具页面的导航栏必须包含同分类的所有工具链接，返回按钮文字统一为"← 返回工具箱"。

---

## 九、开发与上线流程

1. **准备环境**：安装VS Code、Git（可选）
2. **创建文件夹结构**：按第四节的目录创建所有文件
3. **编写代码**：
   - 确保所有页面共用同一份`style.css`和`common.js`
   - 按照第六、七节的设计要求统一风格
   - 智能抠图页面按第五、2节集成组件
4. **本地测试**：使用Live Server测试所有工具功能及导航栏跳转
5. **部署到Vercel**：
   - 将代码推送到GitHub仓库
   - 在Vercel中导入仓库，自动部署
6. **添加自定义域名（可选）**：购买廉价域名，在Vercel中绑定并配置DNS
7. **申请Google AdSense**：确保网站有"关于"和"隐私"页面，提交审核
8. **提交搜索引擎**：在Google Search Console提交sitemap.xml

---

## 十、常见问题与解决方案

| 问题                 | 解决方案                                                     |
| -------------------- | ------------------------------------------------------------ |
| 智能抠图组件不显示   | 检查网络是否能访问CDN；尝试使用国内备选CDN或自托管           |
| 首次抠图很慢         | 正常现象，正在下载AI模型；可在页面添加提示文字               |
| 抠图后图片背景为黑色 | 浏览器不支持WebGPU，已自动降级；可提示用户使用Chrome/Edge最新版 |
| 移动端抠图卡顿       | 建议使用性能较好的手机；可提示“在电脑上体验更佳”             |
| 导航栏在当前页不高亮 | 检查每个页面的`<a>`是否有`active`类                          |
| 移动端导航栏太挤     | 使用`flex-wrap: wrap`，并缩小`gap`                           |
| 圆角不一致           | 统一在全局样式中设置`border-radius`值                        |
| AdSense审核不通过    | 增加原创内容，完善隐私政策                                   |
| 图片水印平铺不均匀   | 调整gap间距值；确保水印图片尺寸比例正确                      |
| 水印透明度不生效     | 检查`globalAlpha`设置顺序，须在`drawImage`/`fillText`之前设置 |
| 导航栏链接不完整     | 确保每个页面包含同分类的所有工具链接，返回按钮统一为"← 返回工具箱" |

---

## 十一、文档更新记录

| 版本 | 日期       | 修改内容                                                     |
| ---- | ---------- | ------------------------------------------------------------ |
| V4.2 | 2026-05-25 | 新增PDF处理类12个工具，总工具数增至33个，新增文档处理分类，更新文件结构，更新技术架构（pdf-lib、pdf.js），新增使用指南页面，智能抠图添加电脑端使用建议 |
| V4.1 | 2026-05-23 | 图片水印工具升级（新增图片水印、平铺、透明度），修复13个页面导航栏（补齐28个缺失链接），明确导航栏须包含同分类所有工具 |
| V4.0 | 2026-05-23 | 新增智能抠图功能（方案一），图片处理类增至6个工具，总工具数21个，调整分类名称顺序 |
| V3.1 | 2026-05-23 | 更新功能列表至18个工具，更新文件结构                         |
| V3.0 | 2026-05-23 | 新增分类导航模式，首页改造                                   |
| V2.0 | 2026-05-23 | 重构为极简横向导航栏风格                                     |
| V1.0 | 2026-05-23 | 初始版本                                                     |

---

## 十二、当前工具总数

**33个工具**，分布在5个分类中：

- 图片处理：6个（压缩、裁剪、格式转换、**水印（文字+图片+平铺）**、尺寸调整、**智能抠图**）
- 文本处理：6个
- 编码转换：4个
- 生成工具：5个
- 文档处理：12个（PDF合并、拆分、提取、删除、旋转、转图片、图片转PDF、压缩、加密、解密、水印、预览）