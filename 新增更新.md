- ## 📁 阶段一：用户体验提升

  ### 1.1 一键置顶按钮

  - **位置**：页面右下角，距离边缘 20px。
  - **样式**：圆形，背景色 `#009DFF`，白色箭头图标，无阴影，圆角 50%，尺寸 44×44px。
  - **行为**：
    - 滚动超过 300px 时淡入显示，否则隐藏。
    - 点击后平滑滚动到页面顶部（`window.scrollTo({ top: 0, behavior: 'smooth' })`）。
  - **技术**：在 `common.js` 中添加全局函数，监听 `scroll` 事件，节流（`requestAnimationFrame`）。
  - **CSS**：`position: fixed; bottom: 20px; right: 20px; display: none; cursor: pointer;`

  ### 1.2 一键复制功能

  - **位置**：每个工具的结果输出区域附近（例如二维码下方、密码框旁边、UUID 显示区）。
  - **UI**：一个小型按钮或图标（📋），点击后显示“已复制！”短暂提示（2秒后消失）。
  - **技术**：
    - 获取结果文本（可能来自 `value`、`textContent` 或 `src` 等，根据不同工具）。
    - `navigator.clipboard.writeText(text)`。
    - 提示可使用一个临时浮动 `toast` 元素。
  - **注意**：对于 canvas 生成的二维码，需先转为 dataURL 再复制？一般不复制图片，复制内容文本（如二维码中的 URL）。可在二维码生成器下方显示 URL 文本并提供复制按钮。

  ### 1.3 暗色模式

  - **开关位置**：页脚或右上角，图标（🌞/🌙）。
  - **实现**：
    - 定义两套 CSS 变量（浅色/深色），通过根元素 `data-theme` 属性切换。
    - 默认跟随系统 `prefers-color-scheme`，但用户手动选择后覆盖。
    - 使用 `localStorage` 存储用户偏好，下次加载时读取。
  - **样式变化**：背景色、文字色、边框色、按钮色等适当调整（保持对比度）。
  - **迁移**：所有现有样式需改为引用 CSS 变量，例如 `color: var(--text-primary);`。

  ### 1.4 移动端触控优化

  - **目标**：确保所有可点击元素在手机上有足够点击区域（`min-width: 44px; min-height: 44px;`）。
  - **调整**：
    - 增加按钮内边距。
    - 增大滑块轨道高度（`height: 6px`，拇指 20px）。
    - 文件上传控件使用自定义样式，避免原生控件过小。
  - **测试**：Chrome DevTools 手机模拟器，实际真机。

  ------

  ## 📁 阶段二：实用计算器与单位换算

  ### 2.1 房贷计算器

  - **页面**：`/calculators/mortgage/`
  - **输入字段**：
    - 贷款总额（万元或元，可切换单位）
    - 年利率（%）
    - 贷款年限（年）
    - 还款方式：等额本息 / 等额本金
  - **输出**：
    - 每月还款额
    - 总利息
    - 还款总金额
    - 可选：显示每月还款明细表（点击展开）
  - **公式**：
    - 等额本息：月利率 = 年利率/12，月供 = 总额×月利率×(1+月利率)^期数 / ((1+月利率)^期数 -1)
    - 等额本金：每月本金 = 总额/期数，每月利息递减。
  - **UI**：表单输入，实时计算或点击按钮计算。

  ### 2.2 车贷计算器

  - **类似房贷**，增加字段：首付比例（%）、购置税（可选，按车价百分比）、保险（固定值）。
  - **页面**：`/calculators/carloan/`

  ### 2.3 日期计算器

  - **页面**：`/calculators/date-diff/`
  - **功能**：
    - 日期差：选择起始日期和结束日期，计算相隔天数、工作日天数（排除周末，可配置节假日？简单版先不排除）。
    - 日期加减：给定起始日期，加减天数，得到新日期。
  - **技术**：使用 JavaScript `Date` 对象，注意时区问题（只处理日期，忽略时间）。

  ### 2.4 单位换算器

  - **页面**：`/calculators/unit-convert/`
  - **分类**：长度、面积、重量（质量）、温度、体积。
  - **UI**：选择类别 → 选择输入单位、输出单位 → 输入数值 → 实时显示结果。
  - **换算映射**：预先定义基准单位（如毫米、平方米、克、摄氏度等），使用转换因子。

  ------

  ## 📁 阶段三：图片处理增强

  ### 3.1 智能图片压缩（按目标大小）

  - **现有页面** `/image-compress/` 增加选项：“目标大小（KB）”。
  - **逻辑**：
    - 用户上传图片后，显示原始大小。
    - 输入目标大小（例如 200），点击压缩。
    - 使用二分法调整质量参数（quality 0-1），每次压缩后检查 Blob 大小。
    - 循环直到大小接近目标（误差 ±10%）或达到最大迭代次数。
  - **注意**：大图片多次压缩可能耗时，需显示进度提示。
  - **边界**：若目标大小比原图还大，直接返回原图。

  ### 3.2 图片格式智能推荐

  - **现有页面** `/image-format/` 增加“推荐格式”按钮。
  - **分析**：
    - 检测图片是否透明：canvas 遍历像素检查 alpha 通道（效率较低，可通过读取 `getImageData` 抽样）。
    - 检测颜色数量：同样抽样估算。
    - 推荐逻辑：
      - 有透明通道 → 推荐 PNG 或 WebP（如果浏览器支持 WebP）。
      - 无透明但颜色少（<256） → 推荐 PNG-8 或 GIF。
      - 其他 → 推荐 JPG 或 WebP。
  - **实现**：点击后弹出提示框，建议格式，用户可一键选择该格式并转换。

  ------

  ## 📁 阶段四：设计辅助工具

  ### 4.1 AI 配色生成器（本地版）

  - **页面**：`/color-palette/`
  - **功能**：
    - 输入关键词（例如“森林”、“科技”），点击生成。
    - 随机展示 5 种颜色，显示 HEX 和 RGB。
    - 每个颜色下方有复制按钮。
    - 可锁定某个颜色，重新生成其他颜色（高级，可后续迭代）。
  - **颜色来源**：预定义关键词映射到几个预设调色板，或使用简单的随机色相（HSL）生成互补/邻近色。例如：主色随机，再通过色相偏移生成其他颜色。
  - **技术**：HSL 操作，生成和谐配色。

  ### 4.2 文案/谐音梗生成器

  - **页面**：`/slogan-generator/`
  - **内容**：预置数组（至少 50 条），随机展示一条。可点击“换一换”。
  - **词库**：收集网络常见广告词、谐音梗，注意不侵权。
  - **UI**：一个卡片显示文本，下方按钮刷新。

  ### 4.3 自定义二维码设计器

  - **改造** `/qr-generator/`，增加样式选项：
    - 前景色、背景色（颜色选择器）。
    - 圆点形状：方形、圆形、圆角方形。
    - Logo 上传（已有）。
  - **技术**：使用 `QRCodeStyling` 库替代原生 QRCode，它支持样式配置和 canvas/ SVG 输出。
  - **下载**：最终输出为 PNG。

  ### 4.4 设计稿模拟器（Mockup 生成器）

  - **页面**：`/mockup-generator/`
  - **功能**：
    - 上传图片（设计稿）。
    - 选择设备：iPhone 15、MacBook Pro、Apple Watch 等。
    - 拖动调整图片在设备屏幕中的位置和缩放（可选，简单版本直接居中填充）。
    - 下载合成图片（PNG）。
  - **实现**：Canvas 绘制，先加载设备边框图（PNG 带透明通道），然后绘制用户图片（缩放、裁剪），最后绘制边框上层（保证阴影等）。需要准备多张设备 PNG 素材（可从免费资源下载，如 `mockupworld`）。

  ------

  ## 📁 阶段五：开发与数据处理工具

  ### 5.1 JSON 编辑器（树形视图）

  - **页面**：`/json-editor/`
  - **功能**：
    - 左侧或顶部输入框（可粘贴 JSON）。
    - 点击“格式化/美化”按钮，右侧显示树形视图，支持折叠/展开、编辑、搜索。
    - 可导出为 JSON 文件。
  - **技术**：使用 `jsoneditor` 库（`npm` 或 CDN），配置 `mode: 'tree'`。
  - **注意**：该库体积较大（~200KB），但可接受。

  ### 5.2 SQL 格式化工具

  - **页面**：`/sql-formatter/`
  - **功能**：输入 SQL，选择格式化风格（缩进大小、大小写转换），点击格式化，输出美化后的 SQL。
  - **技术**：`sql-formatter` 库。
  - **UI**：上下两个文本框（或左右），支持复制。

  ### 5.3 代码/文本差异对比器（增强）

  - **现有** `/text-diff/` 可增强显示效果：
    - 使用 `diff` 库生成差异。
    - 左右两栏并排显示，差异高亮（红色背景删除、绿色背景新增）。
    - 支持行内差异高亮（可选）。
  - **实现**：渲染为 HTML，使用 CSS 样式。

  ------

  ## 📁 阶段六：批量处理与生产力工具

  ### 6.1 批量图片压缩

  - **改造** `/image-compress/` 增加多文件选择（`multiple` 属性）。
  - **UI**：
    - 显示文件列表，每张图片可单独设置压缩质量或目标大小（全局设置）。
    - 压缩后显示每张压缩前后大小，提供“全部下载”按钮（ZIP）。
  - **技术**：遍历文件，使用 `Promise.all` 或串行压缩（避免内存爆炸），完成后再打包 ZIP（`JSZip`）。

  ### 6.2 批量 PDF 拆分增强

  - **修改** `/pdf/split/`，增加“按页数拆分”模式：
    - 输入每 n 页一个文件，自动将 PDF 拆分成多个部分。
  - **实现**：使用 `pdf-lib` 重复提取页面范围。

  ### 6.3 批量重命名工具

  - **页面**：`/batch-rename/`
  - **功能**：
    - 上传多个文件（任意类型）。
    - 输入重命名模式：支持序号占位符 `{#}`，例如 `file_{#}.jpg`。
    - 预览新文件名，可手动编辑个别。
    - 点击“打包下载”生成 ZIP 文件（内部文件名已改）。
  - **技术**：前端无法直接修改原文件名，但可以创建一个新的 `File` 对象（`new File([blob], newName, {type})`），然后打包 ZIP。注意对于图片等，需要读取原始 Blob 内容。

  ------

  ## 通用技术要点

  - **文件组织**：每个功能独立文件夹，例如 `/calculators/mortgage/` 包含 `index.html` 及可能的 `style.css`（或复用全局），`script.js`。
  - **全局组件**：所有页面必须包含顶部“返回工具箱”链接（`<a href="/" class="back-link">← 返回工具箱</a>`）和页脚。
  - **SEO**：每个页面自定义 `<title>` 和 `<meta name="description">`。
  - **性能**：对于耗时的操作（如批量压缩、PDF 拆分），使用 Web Worker 避免 UI 卡顿（可选，初期可不加）。
  - **错误处理**：所有工具应有友好的错误提示（例如文件格式不正确、计算超出范围等）。

  ------

  ## 验收标准

  每个功能开发完成后，应通过以下测试（手动或自动化）：

  1. 在 Chrome、Firefox、Safari 最新版上正常显示和操作。
  2. 移动端（iPhone/Android Chrome）布局正常，触摸响应良好。
  3. 所有交互有明确的反馈（加载中、成功、失败）。
  4. 暗色模式下界面可读。
  5. 纯前端验证：断开网络后仍然可以正常使用（除需要外部库 CDN 外，但库已缓存）。
